# Frontend Environment Variables
# Copy this file to .env.local for local development (Vite ignores .env by default in some setups)
# For production builds (Cloudflare Pages), set these as environment variables in the dashboard

# Backend API URL
# Local development: leave blank or set to http://localhost:3000
# Production (Cloudflare Pages): set to your Railway backend URL
VITE_API_URL=https://your-app-name.railway.app
